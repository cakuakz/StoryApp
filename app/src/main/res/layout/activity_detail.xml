<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.detail.DetailActivity">

    <ImageView
        android:id="@+id/userImgDetail"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@tools:sample/avatars"
        />

    <TextView
        android:id="@+id/usernameDetail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/userImgDetail"
        android:layout_marginTop="30dp"
        android:padding="8dp"
        android:textSize="20sp"
        android:textColor="?attr/colorOnSecondary"
        android:textStyle="bold"
        android:paddingVertical="16dp"
        app:layout_constraintTop_toBottomOf="@id/userImgDetail"
        tools:text="username"
        />

    <TextView
        android:id="@+id/descriptionDetail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:layout_below="@id/usernameDetail"
        android:textSize="16sp"
        android:paddingVertical="16dp"
        app:layout_constraintTop_toBottomOf="@id/usernameDetail"
        tools:text="Description"
        />

</androidx.constraintlayout.widget.ConstraintLayout>